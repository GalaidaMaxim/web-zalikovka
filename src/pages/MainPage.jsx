import { Typography } from "@mui/material";
import { ContainerCustom } from "../components/Container/Container";

export const MainPage = () => {
  return (
    <ContainerCustom>
      <Typography variant="h2">Web заліковка</Typography>
      <Typography variant="body1">проект тестується</Typography>
    </ContainerCustom>
  );
};
